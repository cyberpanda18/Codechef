#include <iostream>
using namespace std;
int main() {
	int t;cin>>t;
	while(t--)
	{
		int arr[5],p,sum=0;
		for(int i=0;i<5;i++)
		cin>>arr[i];
		cin>>p;
		for(int i=0;i<5;i++)
		arr[i]=p*arr[i]-24;
		for(int i=0;i<5;i++)
		sum=sum+arr[i];
		if (sum>0)
		cout<<"Yes"<<endl;
		else cout<<"No"<<endl;
	}
return 0;
}
